{
  "name": "hello-algo-atlas",
  "version": "1.0.0",
  "private": true,
  "description": "A trilingual Korean, Vietnamese, and English interactive learning companion for Hello Algo.",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/check-full-book.mjs && node scripts/check-site.mjs && node scripts/check-javascript-examples.mjs",
    "compare:vi": "node scripts/compare-vietnamese-refs.mjs",
    "start": "npm run build && node scripts/serve-site.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
